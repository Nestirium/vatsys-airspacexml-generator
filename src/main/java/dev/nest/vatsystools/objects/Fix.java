package dev.nest.vatsystools.objects;

import dev.nest.vatsystools.Point;

public final class Fix extends Waypoint {

    public Fix(String identifier, Point coordinates) {
        super(identifier, coordinates);
    }

}
